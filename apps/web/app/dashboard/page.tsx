"use client"; import { useEffect,useState } from "react"; const api=process.env.NEXT_PUBLIC_API_URL||"http://localhost:4000/api"; const money=(v:any)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(Number(v||0)); export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{const t=localStorage.getItem("smartbill_token"),f=localStorage.getItem("smartbill_firm");fetch(`${api}/dashboard/summary`,{headers:{Authorization:`Bearer ${t}`,"x-firm-id":f||""}}).then(r=>r.json()).then(setD)},[]);const cards=[["Total sales",money(d?.totalSales),"bg-blue-600"],["Receivables",money(d?.receivables),"bg-amber-500"],["Stock units",String(d?.totalStock??"—"),"bg-emerald-600"],["Low stock",String(d?.lowStock??"—"),"bg-rose-600"]];return <><p className="font-semibold text-blue-600">OVERVIEW</p><h1 className="mt-2 text-3xl font-bold">Business dashboard</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(c=><div key={c[0]} className={`${c[2]} rounded-2xl p-5 text-white`}><p className="text-white/80">{c[0]}</p><p className="mt-3 text-2xl font-bold">{c[1]}</p></div>)}</div><section className="mt-8 rounded-2xl bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Get started</h2><p className="mt-2 text-slate-600">Create customers and products, then issue your first GST invoice.</p></section></>}