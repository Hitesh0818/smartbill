import { Controller, Get, Headers, UseGuards } from "@nestjs/common"; import { JwtAuthGuard } from "../auth/jwt-auth.guard"; import { AuthUser } from "../auth/auth-user.decorator"; import { DashboardService } from "./dashboard.service"; @UseGuards(JwtAuthGuard) @Controller("dashboard") export class DashboardController {constructor(private service:DashboardService){} @Get("summary") summary(@AuthUser()u:{sub:string},@Headers("x-firm-id")f:string){return this.service.summary(u.sub,f)}}